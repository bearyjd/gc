GC_EMAIL="you@example.com"
GC_PASSWORD="yourpassword"
GC_CALENDAR_ID="your-calendar-id@group.calendar.google.com"
# GOG_ACCOUNT="you@gmail.com"
# GC_TOKEN="eyJ..."              # Skip Playwright entirely (required if MFA blocks headless login)
# GC_DEVICE_ID="a85ba962..."    # Optional alongside GC_TOKEN — helps GC recognize the device, may skip OTP
                                 # Get both from DevTools → Network → any api.team-manager.gc.com request headers
